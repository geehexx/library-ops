# Task Master PRD Compatibility Shim

`docs/PRD.md` is the sole canonical PRD for Library Ops.

This file is not a second source of truth. It exists only as a compatibility
marker for older local Task Master expectations while the repo migrates fully
to `docs/PRD.md`.

Use this command when refreshing the execution graph:

```bash
npx --yes --package task-master-ai@0.43.1 -c 'task-master parse-prd docs/PRD.md --research'
```

Rules:

- Do not edit this file to change product requirements.
- Do not parse Task Master from this file.
- Keep phase PRDs, runtime policy, AGENTS, and repo docs aligned to
  `docs/PRD.md`.
