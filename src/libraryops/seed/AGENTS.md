# Seed/import subsystem rules

- Demo data must be reproducible.
- Import commands must support `--dry-run`, `--limit`, and safe `--refresh`.
- Store external source provenance.
- Do not scrape sources that prohibit bulk download.
- Keep imported corpora out of git.
