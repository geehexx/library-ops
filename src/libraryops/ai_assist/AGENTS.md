# AI assistance subsystem rules

- AI output must be grounded in stored catalog records.
- AI may suggest metadata but must not auto-persist without review.
- AI must never invent availability.
- Prefer local embeddings for demo reliability.
- Store provenance for generated suggestions.
