interface:
  display_name: "Search Design"
  short_description: "Design exact, PostgreSQL FTS, and lexical search"
  default_prompt: "Use $search-design to design or review the Library Ops search stack across exact identifiers, PostgreSQL FTS, and lexical ranking."

dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Context7 MCP server for current database and library documentation"
    - type: "mcp"
      value: "exa"
      description: "Exa MCP server for current-source search-system research"
